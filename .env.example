# Future provider/persistence adapters read server-side environment only.
# The initial quality-gate ticket does not need credentials or a database.
SERVER_PORT=8080
# DATABASE_URL=jdbc:postgresql://localhost:5432/worldcup
# DATABASE_USERNAME=worldcup
# DATABASE_PASSWORD=
# CANDIDATE_MODEL=
# CANDIDATE_API_KEY=
