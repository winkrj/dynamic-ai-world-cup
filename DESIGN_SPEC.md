# Design Spec — 구현 기준 v1.1

화면 정책은 최신 대화 결정의 복원본이다. 2026-09-18 사용자가 `AI World Cup 시안4.html`을 제공했고 대결은 **A — SPLIT DECK(상하 카드)**를 선택했다. 시안의 색상·타이포·카드와 아래 제품 규칙을 결합한 구현 기준은 [제품 통합 Spec](docs/INTEGRATION_SPEC.md)이다. 시안의 고정 8강 데모·편집 가능한 타이머·가짜 진행률을 실제 동작으로 채택하지 않으며, 최초 스캐폴드 UI나 소스 분석을 최종 렌더링 승인으로 간주하지 않는다.

## 화면

| 화면 | 내용 | 주 행동 / 상태 |
| --- | --- | --- |
| 고민 입력 | 짧은 제목, 자연어 입력, 예시 3개, 조건을 함께 적을 수 있다는 안내 | 월드컵 만들기. 로그인/필터 폼/온보딩 없음 |
| 강수 | 8강=7번, 16강=15번, 32강=31번 선택 | 16강 기본 강조, 사용자가 선택 |
| 생성 중 | 빈 대진 카드가 채워지는 연출, 상태 문구 | 취소/재시도는 생성 상태에 맞춤; 미검증 후보 이름을 먼저 노출하지 않음 |
| 미리보기 | N개 전체 후보, 이름/태그, 재생성 잔여 1회 | 이대로 시작 / 전체 다시 만들기. 전체 bracket tree 불필요 |
| 대결 | 라운드·진행 번호, 카운트다운 바, 큰 A/B 카드, VS | 카드 전체가 선택 버튼 |
| Champion | 우승 후보, 간결한 결과 | 공유하기 |
| 공유 진입 | 만든 사람의 우승, 같은 후보군 | 이 월드컵 해보기; 새 세션 |

## 카드와 움직임

- 기본: 이름 + 이미지 또는 동일 크기의 대체 카드 + 핵심 태그 최대 2개.
- 모바일 360px부터 사용 가능하게 만들며 데스크톱에서도 선택한 A안 상하 카드를 유지한다.
- 터치 목표 최소 44px, 키보드 선택 가능, 색만으로 승패 표시하지 않음, 가로 스크롤 없음.
- 진입 약 300–450ms, 선택 피드백 약 200–350ms, 라운드 전환 ≤500ms. reduced-motion은 움직임을 생략하되 준비 시점을 유지한다.
- 두 카드의 렌더링/이미지 또는 fallback 확정/진입 연출 완료 후 타이머를 시작한다. 이미지 실패/2초 로딩 초과는 fallback으로 확정해 무한 대기하지 않는다.
- 실제 제한시간은 snapshot의 `rules.matchDurationMs`=7000. 결승도 같은 컴포넌트와 규칙을 사용한다.
- 시간 초과 때 짧게 “시간초과 · 랜덤 진출”. 별도 랜덤 결과 화면이나 사용자 확인은 없다.
- 결과 확정 즉시 입력 잠금. 빠른 두 번 탭·timeout 경합으로 2개 승자를 만들지 않는다.

## 예외 정책 (이번 기술 설계 선택)

- 모호해서 같은 Candidate Unit을 정할 수 없으면 가장 필요한 보충 질문 1개. 필수 다단계 설문 없음.
- 초기 생성의 `CLARIFICATION_REQUIRED`는 제품 고정 질문으로 비교할 대상/활동을 묻는다. 원래 고민·강수와 답변을 보존하며 사용자가 제출할 때만 합쳐서 새 요청으로 접수한다(총 500 Unicode 글자, 무단 축약 없음). 흐름당 질문은 최대 1회이고 반복 실패는 입력 수정으로 돌아간다. 새 접수는 하루 2회 한도에 포함되며 동일 요청 재전송은 추가 차감하지 않는다. 모델이 작성한 질문이나 서버 대화 이력이 있다고 표시하지 않는다.
- 현재 사실을 확인할 수 없으면 근거를 만들어내지 않는다. 내부 Repair 후에도 불가능하면 실패와 조건 수정 안내.
- 재생성 중 기존 미리보기를 보존하고 시작/재생성을 잠근다. 실패 시 기존 미리보기로 돌아오며 서버 오류 재시도는 동일 operation으로 한다.
- 플레이 중 화면이 숨겨지면 현재 경기의 실제 deadline은 유지한다. 복귀 때 만료한 **현재 경기 한 개만** timeout 처리하고 다음 카드는 표시 준비부터 시작한다. 숨겨진 상태에서 전체 월드컵을 자동 소진하지 않는다.
- 새로고침은 로컬 저장한 frozen snapshot/세션/현재 deadline을 복원한다. 만료 규칙도 동일하다.
- 공유 snapshot 조회 실패는 없는 링크/일시적 장애를 구분한다. 이미지/링크가 나중에 사라져도 후보나 대진을 변경하지 않는다.
