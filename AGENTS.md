# 공통 AI 개발 지침

## 심사 모드·반복 배포 — TD-57 (2026-09-20)

사용자가 심사 목적으로 일일 생성 횟수 제한 해제와 쉬운 배포 파이프라인/아키텍처 정리를 요청했다. `GENERATION_DAILY_LIMIT=0`은 일일 cap만 해제하며 기본2·양수 동작과 회계 기록, actor/IP burst5회/10분, 운영 누적$5·실험$6·자동충전OFF·재생성1회·게임 규칙을 유지한다. 일일 잔여와 전체 재생성 잔여를 혼동하지 않는다. 프론트는 설정과 무관하게 참인 안내를 쓰며 심사 모드를 무제한 과금으로 설명하지 않는다. 운영 runtime은0/2만 허용하고 누락은2로 유지한다.

배포는 기존 단일 호스트/DB/비밀설정/자원을 유지해 검증→immutable image→백업→SSM 교체→HTTPS 확인을 재사용 가능한 진입점으로 묶는다. DB 자동 복원·예산 초기화·다른 프로젝트 변경은 금지다. GitHub OIDC 역할과 기본 브랜치 반영/버튼 배포 활성화는 사용자 별도 답변을 기다린다. 해당 권한 대기와 독립적인 코드·테스트·기존 권한 로컬 배포는 진행한다. 실제 활성화/배포 성공 전 구성 파일 존재만으로 완료라 말하지 않는다.

## 후속 플레이 연출 — TD-56 (2026-09-20)

소스 `521fbfe` 구현·최종 verify·합성 브라우저(8강28경기/16·32강184경기)·독립 리뷰1차 Critical/High/actionable0·기존 AWS 배포 완료. 공개 JS/CSS가 검증 파일과 바이트 일치하며 실제 공유16강의 직접7/timeout8·Champion저장·오류0을 확인했다. 운영 장부14건/$0.73137410·snapshot 전체 hash는 배포/플레이 전후 같고 추가AI0이다. 증거와 최신 digest는 docs/VERIFICATION.md, docs/V1_SUBMISSION.md를 따른다. 사진은 설계만이며 현재 카드 이미지가 구현됐다고 설명하지 않는다.

사용자는 HTML 시안과의 연출 차이 확인, 더 긴박한 선택, 라운드 전환 인지를 요청했다. 변경은 UI와 합성 브라우저 검증에 한정한다. 라운드 첫 preparation500ms 안내·상시 강수 경로·고정 button 내부900ms 압박·마지막3초 강조를 추가하되 실제7초/feedback280ms/이미지2초fallback/기존deadline복원/숨김·잠금 보호를 유지한다. reduced-motion은 정적인 같은 안내다. 사진은 docs/CANDIDATE_VISUALS.md의 별도 설계만 작성하며 수집/생성/API가입/새과금/계약변경은 이번에 하지 않는다. 엔진·DB·예산·핵심게임은 변경하지 않는다. 코드검증과 실제 배포는 구분한다.

## 제출 마감 — TD-55 (2026-09-20)

사용자는 아침 팀원 전달을 위한 **운영 가능한 1차 완성**을 요청했다. TD-54 `catalog`를 기존 AWS 운영에 적용하고 시안 A의 누락 애니메이션을 보완한다. 새 모델/프레임워크/인프라나 기능 확장보다 통합·배포·전달을 우선한다. 진행 상태와 완료 증거는 `docs/V1_SUBMISSION.md`가 최신 진입점이다.

현재: 2026-09-20 소스 `fcb6a5f`의 catalog·애니메이션을 기존 AWS에 배포 완료했다. 전체 verify(Java452 실행/유료2 제외, frontend61)·합성 브라우저8/16/32·독립 리뷰1차 Critical/High/actionable0를 거친 동일 이미지다. 운영 V4/seed64·HTTPS·기존 snapshot hash 보존·백업·16강 저장/공유·실제 브라우저 timeout15경기/Champion/공유를 확인했다. DB preset 0.767초/AI0, 조건부16강 preview 5.357초/provider1회/$0.012404는 단일 관측이다. 운영 누적$0.697184/$5, 실험$5.4206158/$6으로 분리한다. v3 32강 의미 유사성은 남는다. 초기 임시 HTTP 점검기의 잘못된 공유 본문으로 발생한400은 기록하고, AI 재호출 없이 DB preset으로 정상 공유를 확인했다. 최신 증거/팀원 안내는 docs/V1_SUBMISSION.md다.

- 목표: DB preset 사용자 대기 2초 이내, 일반 16강 AI 생성 약 10초, 관측 비용 건당 $0.05 이하. 이는 소규모 측정 목표이며 p95/SLA/청구 hard cap이 아니다. catalog 호출 deadline은 30초, 요청당 AI 0~1회이며 자동 검색/Repair/fallback은 없다.
- 품질은 명시 조건 우선·N/ID/이름/family/구조 검사·미리보기로 판단하며 평범함과 호불호는 허용한다. 의미 적합성의 best-effort 한계와 최신 사실 확인 미지원은 숨기지 않는다. 16강 기본·8/16/32 지원은 그대로다.
- 프론트 진입 350ms·결정 피드백 280ms·실제 선택 7000ms를 유지한다. 원본 연출을 그대로 길게 복제하지 않고 그 안에 맞춘다. reduced motion·키보드·360px·이미지 fallback을 검증한다.
- 운영 누적 $5 / 실험 누적 $6 / 자동 충전 OFF / 하루 2회는 유지한다. DB·비용 장부·기존 공유를 보존하고 새 migration은 추가만 한다. 기존 AWS 계정 로그인 만료 등 사용자 단계는 배포만 보류하며 개발/검증을 계속한다.
- 이번 추가 UI/runtime 변경은 새 relevant test와 독립 리뷰(최대 2회)가 필요하다. TD-54의 완료된 리뷰를 이번 변경에 재사용하지 않는다. 최종 Git revision·이미지 digest·HTTPS 실제 결과 전 운영 적용 완료로 쓰지 않는다.

## 최신 제품 전환 — TD-54 (2026-09-19)

사용자는 30초의 최초 생성도 길다고 판단하고, 완벽한 후보를 반복 검증하기보다 **DB 후보 우선 + 프롬프트 중심의 단일 선택/보충 호출**로 속도·비용과 충분히 좋은 선택 경험을 개선하도록 요청했다. 이 명시 결정은 아래 TD-51의 온라인 독립 검토 필수·후보 pool 조합 금지를 새 `catalog` 경로에 한해 대체한다. 기존 `staged` 경로와 과거 FAIL/사람 판정은 그대로 보존한다.

- `catalog`: 편집된 정확한 preset은 AI 0회. 임의 자연어는 DB 검색 후 ID 선택·부족분 생성 최대 1회, 검색/Repair/독립 재검토/느린 엔진 자동 fallback 없음. 명시 조건은 프롬프트 최우선이지만 의미 적합성은 best-effort이며 완벽한 준수를 보증하지 않는다. 서버는 N·ID·이름/family 중복·필드·단위·구조를 검사한다.
- `FAST_BEST_EFFORT` 정책과 `fast-best-effort-v1` provenance를 명시한다. 생성자 판단을 독립 PASS evidence로 위장하거나 기존 재사용 certificate를 발급하지 않는다. Coverage는 구성 결과의 집계이며 독립적으로 승인된 계획이 아니다.
- 새 공용 카탈로그는 일반적인 취미/데이트 활동/식사 아이디어의 AI 편집 seed다. `human_rated=false`를 유지하고 과거 보류 세트·개인 prompt·새 생성 결과를 자동 승격하지 않는다. 최신 장소/가격/영업 근거 확인이 필요한 입력은 빠른 경로에서 확인했다고 주장하거나 활동으로 몰래 바꾸지 않는다.
- 8/16/32·전체 preview·성공 재생성 1회·freeze·7초·공유 불변·익명 일일 2회·예산 예약은 유지한다. 운영 전략 기본값은 측정/검토 전 `staged`; 배포 적용과 로컬 구현/실험 완료는 구분한다.
- 실험은 고정된 공개 합성 사례로 한 번씩 비교하고 실패/이상 후보도 보존한다. 기존 누적 실험 $6(직전 $5.2955198, 미확인 $0.50 포함), 검색 없음, 자동 충전 OFF, .50 호출 예약을 유지한다. 같은 실패 수정/재검증 최대 3회, 독립 코드 리뷰 최대 2회. 통과할 때까지 무한 재추첨하지 않는다.
- 상세 설계/판정 목표/실험 결과는 `docs/CATALOG_ENGINE.md`. 이전 승인·숫자는 각 시점의 기록이다.

TD-54 실행 완료(로컬): 64개 seed·0/1회 경로·451 Java 실행/유료 2개 제외·전체 verify·최종 독립 리뷰 Critical/High/actionable 0. 실제 8개 실험/유료 7회/검색 0/추가 $0.09224, 최신 실험 장부 **$5.3877598/$6**(미확인 .50 포함), 잔여 $0.6122402다. v1 식사 일반 쌀국수·샤부샤부 조건 문제는 한 번의 프롬프트 보완 후 v2로 재비교했으며 첫 내용 보류를 보존한다. v2 취미 2.147초, 식사 6.368초는 단일 관측이며 전체 의미 품질/운영 SLA가 아니다. 운영/원격 Git 적용 없음. 이후 코드 변경은 이번 최종 리뷰 결과를 재사용하지 않는다.

## 배포 후속 승인 — TD-53 (2026-09-19)

사용자가 기존 프로젝트 로컬 OpenAI 키를 현재 배포 AWS 계정의 서울 Parameter Store `/worldcup/production/app.env` SecureString으로 이전하는 데 명시 동의했다. 해당 계정 관리자와 운영 인스턴스 역할이 접근할 수 있음을 설명한 뒤 받은 승인이다. 키를 채팅·Git·명령 인자·로그에 출력하지 않고 다른 계정/수신지로 확대하지 않는다. 마지막 AWS 호환 수정 `13e55a1`의 독립 검토도 기존 상한을 넘겨 **추가 1회** 승인했다. 리뷰 무제한 재개가 아니며 TD-52의 예산·보호 경계·실제 완료 증거는 그대로다. 이전 키 이전/추가 리뷰 승인 대기는 당시 상태다.

후속 실행 완료: 키 이전·추가 리뷰(Critical/High/actionable 0)·실제 HTTPS smoke·16강 1회 생성/15경기/공유/새 세션·앱 재시작 후 snapshot 보존·S3 다운로드/격리 복원·한국 03시 백업 timer를 확인했다. 운영 provider 4건/검색 0/$0.14619000, 누적 $5 중 잔여 $4.853810 기준이며 이후 사용은 실제 운영 장부를 조회한다. 실험 장부와 합쳐 한도를 재설정하지 않는다. 운영 코드 revision/digest·남은 32강 품질/단일 호스트 한계는 docs/DEPLOYMENT.md와 docs/VERIFICATION.md를 따른다. 추가 리뷰 승인은 소진됐으며 이 결과를 이후 코드 변경의 검토로 재사용하지 않는다.

## 최신 배포 승인 — TD-52 (2026-09-19)

사용자가 AWS 공개 배포와 **별도 운영 AI 누적 총한도 $5**를 승인했다. 월별 반복 예산이 아니며 기존 실험 장부 $5.2955198/$6과 분리한다. 자동 충전 OFF, 익명 브라우저 하루 2회, 서버 예약·미확인 비용 보존, 기존 모델·품질·게임 규칙을 유지한다. AWS Free plan/적용 자원을 확인하고 기존 다른 프로젝트 자원은 변경하지 않는다. 새 결제·유료 플랜 전환·도메인 구매는 포함하지 않는다. 운영 DB 복원/교체로 예산을 초기화하지 않는다. 아래 운영 예산·배포 미승인은 각 시점의 역사 기록이며 이 승인 범위에서만 갱신한다. 실제 HTTPS·프록시 제한·DB 보존/백업/복원·최종 이미지 smoke가 끝나기 전 배포 완료로 보고하지 않는다.

## 최신 호출 구조 — TD-51

사용자가 호출 구조 단순화를 명시 승인했다. v23은 조건/선호→비교 단위→관련 이력→coverage→전체 후보를 한 구조화 응답으로 생성하고, 필요한 selective grounding 뒤 별도 요청으로 전체 세트와 해석을 검토한다. 후보 문제는 고정 계획 아래 Repair 최대 1회 후 전체 재검토한다. 해석 FAIL/UNKNOWN은 조건을 바꿔 수리하지 않고 종료한다. 사전 ALLOCATE/REPAIR_INTENTS는 실행 경로에서 제거하며 남은 allocate adapter는 소진한 과거 진단 전용이다. 모델·8/16/32·공개 API·게임 규칙·누적 $6/검색 없음/자동 충전 OFF·기존 토큰/시간/예약 한도는 유지한다. 재사용 정책은 v4-engine-v23: 실행하지 않은 사전 판정을 PASS로 합성하지 않고 최종 원본 검토 증거만 저장한다. 구 증명은 재사용 miss이며 기존 frozen snapshot/공유는 보존한다. 아래 v22/이전 단계와 결과는 역사 기록이다.

## 최신 실험 사전 승인 — TD-50

사용자가 앞으로 필요한 실제 평가를 한 번씩 진행하는 데 사전 승인했다. 목적과 변경/관찰 대상이 명확한 실험은 매번 재질문하지 않고 순차 1건씩 실행한다. 기존 누적 $6·검색 없음·자동 충전 OFF·모델·Repair/호출 예약 한도는 유지하며 운영 트래픽/자동 충전/상한 인상/무한 재추첨으로 확대하지 않는다. 한 평가 안의 기존 단계별 provider 호출 수와 실험 횟수를 구별해 비용을 합산한다. 일반 검증은 두 유료 플래그 false, 실행 때만 해당 자식 프로세스에 주입한다. 소진한 고정 TD-48 진단은 재사용하지 않는다. 동일 실패 수정–재검증 최대 3회 및 review 최대 2회는 유지한다. 아래 '추가 미승인/승인 소진'은 각 실험 당시 기록이고 이후 신규 실험 승인은 이 절이 우선한다.

현재 장부 $5.2955198/$6(미확인 예약 $0.50 포함), 잔여 $0.7044802이다. v23 hobby-solo/16 실제 1회는 Repair 1회 뒤 READY·15경기 저장·동일 snapshot 공유/새 세션까지 PASS, 127.012초/4회/$0.1380685/검색 0이었다. 후속 v23/32 첫 관측은 185.026초/4회/$0.221852/Repair 1회/검색 0, 최종 그림 3개 중복 FAIL로 QUALITY_GATE_FAILED다. 출력 잘림/시간 초과는 없었지만 preview·완주 없음, 재추첨/프롬프트 수정 없음이다. Java 358개·프론트 60개·전체 verify·독립 리뷰 Critical/High 0은 같은 코드의 오프라인 증거이며 별도 실제 32강 테스트 FAIL을 덮지 않는다. 매 실험 전 전체 장부를 다시 합산하고 격리 DB의 새 예산을 누적 상한으로 착각하지 않는다. 선불 잔액 차단에는 처리 지연이 있을 수 있으므로 잔액 소진에만 의존하지 않고 기존 서버 예약/상한을 유지한다.

## 최신 1차 버전 기준 — TD-49

사용자는 모든 입력에서 32개 전부가 매력적일 필요는 없으며 일부 평범하거나 호불호 있는 후보를 허용하는 기준으로 1차 버전을 마무리하도록 했다. 16강 기본과 8/16/32 지원을 유지한다. 주관적 매력 부족만으로 후보를 탈락시키지 않되, 명시 조건·의미 중복·비교 단위·취미 지속성·실행 가능성·필수 근거 검사는 그대로다. 생성/검토 공통 지침에서 두 기준을 분리하며 서버가 FAIL/UNKNOWN을 PASS로 덮지 않는다. 32강 제거·자동 축소는 포함하지 않는다. 전체 입력 성공 보장이나 과거 FAIL 소급 승인을 완료 조건으로 삼지 않으며 코드 검증, 실제 모델 관찰, 운영 배포 상태를 구별한다.

후속 승인한 v22 `hobby-solo/16` 실제 연결 검증 **1회는 소진**했다. 47.849초/4회 HTTP 200/$0.063963/검색 0/사전 Repair 1회, 재검토의 UNIT 해석 FAIL 및 승인 15/16으로 QUALITY_GATE_FAILED다. preview·저장·공유 검증에 도달하지 않았고 재시도하지 않는다. 누적 장부 $4.9355993/$6(미확인 예약 $0.50 포함), 자동 충전 OFF 확인. 두 유료 플래그는 기본 false이며 잔여액 $1.0644007은 새 호출 승인이 아니다. Java 327개·프론트 60개·전체 verify 및 독립 리뷰는 PASS지만 실제 생성 연결/배포 완료로 보고하지 않는다.

## 최신 승인 — DB 재사용·서비스 마감·배포

- TD-48 조건 해석 진단 1회 승인 **소진**: 고정 v20 첫 ALLOCATE 입력을 v21 검토기에 그대로 전달했다. 1회 HTTP 200/17.382초/$0.021828/검색·Repair·새 생성 0, 제외 조건이 soft에만 기록된 오류를 CONSTRAINTS FAIL로 검출해 진단 테스트는 PASS다. 전체 후보 품질 통과·새 preview·중복 해결은 아니다. 누적 장부 $4.8716363/$6(미확인 예약 $0.50 포함), 자동 충전 OFF. 이후 두 유료 플래그는 false로 유지하고 고정 진단 경로를 삭제/재사용하거나 추가 호출하지 않는다. 아래 금액과 미실행 표시는 당시 기록이다.
- TD-47은 추가 과금 없는 제외 조건 분류 보완이다. v21에서 직접 거부·상대적 선호·부정문의 대조 기준을 생성/검토 공통 지침에 넣고 재사용 정책을 v3-engine-v21로 분리한다. 모델/schema/API/Repair/시간·예산은 변경하지 않는다. 이 변경을 중복 문제 해결·실제 의미 품질 통과·새 유료 실행 승인으로 해석하지 않는다. 반복된 중복 실패는 새 버전명으로 수정–재평가 한도를 초기화하지 않으며 사람 기준 대조와 별도 실험 판단 전 유료 재추첨하지 않는다.
- TD-46 승인 **소진**: v20 `hobby-solo/32` 검색 없는 실제 평가 1회는 75.395초/4회 HTTP 200/$0.08744, 사전 Repair 1회 뒤 승인 활동 30/32로 `QUALITY_GATE_FAILED` 종료했다. 상세 후보/preview/완주/공유 없음, 재시도 없음. 현재 누적 장부 $4.8498083/$6(미확인 예약 $0.50 포함), 잔여 $1.1501917은 새 호출 승인이 아니다. 자동 충전 OFF, `CANDIDATE_LIVE_TEST=false` 유지. 사전 환경 PASS를 최종 feasibility·사람 품질 통과로 보지 않는다. 생성·검토의 비운동 조건 분류 및 중복 판정 불일치는 남았다.
- 사용자가 DB 재사용 검증 후 서비스 개발 마무리와 배포를 승인했다. 아래 과거의 'DB 제안만/배포 범위 밖'은 이번 범위에 한해 갱신한다. 기존 게임 규칙과 일일 2회 정책은 유지한다.
- 우선 정확한 요청 조건과 정책 버전이 일치하는 **명시 승인된 세트**를 DB에서 재검증해 재사용한다. 유사 문장 자동 승인·후보 pool 임의 조합·과거 FAILED/보류 데이터의 승인 승격은 하지 않는다. 결과 저장과 사람의 공용 재사용 승인은 구분한다.
- 서비스 마감에는 누락 기능·실행 정보·배포 설정/상태 확인을 포함한다. 계약 변경은 필요한 호환 확장과 프론트 영향 검증을 함께 한다. 최신 비용 기준(TD-45)은 AWS Free plan 안에서 실제 결제 $0를 우선하는 것이다. 크레딧 소비와 실제 결제를 구분하며, $25는 확정 견적/승인 예산이 아니다. 구체 자원·사양·무료 적용은 생성 전 확인하고 유료 플랜 전환이나 이를 유발하는 계정 변경은 하지 않는다. 운영 AI 예산·추가 유료 평가는 별도 미승인이다.
- 사용자가 검색 없는 v19 hobby-solo/32 실제 평가 **추가 1회**를 승인했고 실행 종료했다. 최종 UNIT 해석 FAIL·베이킹 환경 UNKNOWN으로 실패, 6회/176.932초/$0.238354/검색 0/사전 Repair 1회/preview 없음. 누적 장부 $4.7623683/$6(미확인 예약 $0.50 포함). 승인 소진, 새 승인 전 모든 검증은 `CANDIDATE_LIVE_TEST=false`. 자동 충전 OFF를 변경하지 않는다.
- 최신 사용자 기준(TD-44): 취미를 위해 오븐 등을 구입할 수 있으므로 현재 장비 소유 미기재만으로 UNKNOWN을 만들지 않는다. 구매·준비·기초 학습으로 마련 가능한 전제와 아파트 창밖 새처럼 구매로 확보할 수 없는 외부 환경을 구별한다. 명시 예산/공간/소음/시간 조건은 계속 적용한다. v20 프롬프트·재사용 정책에 반영하며 v19 실패를 소급 승인하거나 v20 실제 평가로 집계하지 않는다.

## 최신 추가 범위 — 2026-09-18 TD-39/40

TD-41/v19는 unit을 후보 종류·비교 수준으로 정의하고 constraints/softPreferences의 역할을 공통 프롬프트에서 분리하는 비용 없는 보완이다. 사용자 선택 범위·명시 조건·선호를 보존하고 해석 FAIL/UNKNOWN을 코드에서 완화하지 않는다. 새 유료 호출/DB 재사용 구현 승인으로 해석하지 않는다.

- 사용자가 실험 누적 상한 $6과 검색 없는 v18 hobby-solo/32 1회를 승인했고 **실행 완료**했다. 2회 응답 뒤 사전 해석 UNIT FAIL로 종료, preview/Repair/검색 없음. 추정 $0.0728365, 누적 장부 $4.5240143(미확인 예약 $0.50 포함), 잔여 $1.4759857. 자동 충전 OFF 유지, 새 승인 전 추가 유료 시도 금지. 아래 $5 기록은 이전 실행 당시 기준이다.
- 초기 익명 브라우저별 하루 2회 새 생성/재생성 접수, 서울 자정 초기화를 확정했다. 접수 후 FAILED 포함, 재전송/접수 rollback/Repair/공유 플레이 추가 차감 없음. 기존 burst/IP와 전역 예산 통제를 유지한다. 일일 횟수를 정확한 사람당 식별이나 운영 월 예산 승인으로 혼동하지 않는다.
- DB 우선 후보 재사용은 사용자의 후속 제안으로 TECH_DESIGN 8절에 기록했지만 아직 미구현·미확정이다. 생성됐다는 이유만으로 보류 후보를 공용 승인 pool에 넣거나 무승인 사전 생성·학습·새 DB를 시작하지 않는다.

## 먼저 읽기

`PROJECT_CONTEXT.md` → `PRD.md` / `DESIGN_SPEC.md` / `ACCEPTANCE_CRITERIA.md` → 해당 `TECH_DESIGN.md` / `contracts/openapi.json` → 자신의 역할 지침. 출처 한계는 `docs/SOURCE_PROVENANCE.md`, 최신 변경은 `DECISIONS.md`다.

## 큰 역할 두 개

- A: 플레이 경험 전체. `frontend/**` 소유.
- B: 후보 품질과 서버 전체. `backend/**`, `evals/**`, `scripts/**`, CI/루트 빌드 소유.
- 공통 접점은 `contracts/**`. B가 변경 작성, A가 영향 검토. 생성된 `frontend/src/api/schema.d.ts` 직접 수정 금지. 상세는 `docs/COLLABORATION.md`.
- 자신의 AI에게 티켓과 소유 경로를 명시한다. 공유 파일 변경이 필요하면 양쪽 영향부터 기록한다. 구현은 role별 feature branch, 안정 통합은 main.

## 현재 통합 작업 — 2026-09-18

사용자가 프론트까지 직접 맡기로 하여 A/B 모두 현재 작업 범위다. 더 이상 외부 프론트 인계를 기다리지 않는다. `feat/product/split-deck-flow`에서 아래 책임을 분리해 병렬 작업한다. 같은 checkout을 공유하므로 에이전트는 브랜치 전환·commit·push·타인 파일 수정 없이 담당 변경을 전달하고 통합 담당자만 Git을 조작한다.

| 역할 | 소유/판정 책임 | 넘어서는 안 되는 경계 |
| --- | --- | --- |
| PM / 통합 담당 | 루트 지침·문서·빌드·`frontend/src/app/**`·`main.tsx`, 계약 정합성·최종 검증 | 화면 완주를 후보 품질/배포 승인으로 선언하지 않음 |
| 프론트 상태 담당 | `frontend/src/play/**`, API client/types, 해당 단위 테스트 | React 연출·서버 엔진 변경 없음 |
| 디자이너 / UI 담당 | `frontend/src/ui/**`, style/index, 접근성·반응형 | A 상하 카드 유지, 가짜 API 상태·후보 설명·진행률 발명 금지 |
| 백엔드 담당 | 서버 API/저장 및 HTTP 통합 테스트, 아래 재개 범위의 후보 엔진 | 공개 계약·모델·예산 임의 변경 및 승인 범위 밖 유료 실험 금지 |
| 독립 Reviewer | 변경/테스트를 읽고 결함과 회귀 판정 | 구현자의 동의나 테스트 통과만으로 승인하지 않음; 수정하지 않음 |

- 구현 전 공용 타입/함수/오류·재시도 의미를 합의한다. 소유 파일이 겹치면 동시에 수정하지 말고 통합 담당자에게 구체적인 변경을 요청한다.
- PM은 PRD/AC 누락, 디자이너는 실제 사용 불가, 백엔드는 저장·보안 불변성, 프론트는 상태·복원 경합에 대해 **근거 있는 반대 의견**을 낸다. 취향을 결함으로 포장하거나 무조건 동의하지 않는다. `문제 → 파일/AC 근거 → 사용자 영향 → 가장 작은 대안`으로 설명한다.
- 충돌 우선순위는 최신 명시 사용자 결정 → 제품 불변 조건/AC → OpenAPI wire 계약 → 통합 Spec → 시안 데모다. 제품 규칙이나 계약 의미를 바꾸는 충돌은 통합 담당자가 기록하고 필요할 때만 사용자에게 묻는다.
- 완료 범위는 no-login 입력→8/16/32 preview/재생성→N−1 경기→서버 저장→공유·새 세션이다. 후보 사람 평가 FAIL·외부 사실 eval·운영 배포는 별도 미완료로 남긴다.
- 새로운 유료 호출/키 사용, API 예산 완화, 자동 충전, main 병합/공개 배포는 이번 화면 통합에 포함하지 않는다. 기존 실제 API client를 사용하되 검증은 합성 dev 서버/격리 DB로 한다.

## 엔진 수정 재개 — 2026-09-18

사용자가 수정 보류를 해제했다. 완성된 제품 흐름을 기반으로 `feat/engine/context-feasibility`에서 추가 과금 없는 후보 품질 보완을 진행한다. 위 화면 통합 당시 엔진/프롬프트 동결은 이 범위에서 해제한다. 후보별 필수 접근 전제의 미확인 상태를 별도 내부 판정으로 다루며, 일반 준비물을 모두 미확인으로 거절하거나 특정 취미를 금지하지 않는다. 공개 DTO·모델·호출 수·Repair 합계 1회·비용 예약은 유지한다. 실제 유료 재평가, 사람 품질 승인, main 병합/배포는 이 코드 검증과 구별한다. 기존 사람 평가 FAIL을 자동 PASS로 덮어쓰지 않는다.

## 실행 명령

2026-09-18 품질 기준(TD-38): 사용자는 소음 제한이 없는 요청의 하모니카 반복 연습은 집에서 가능하다고 확인했다. 일반적인 집 안 공간·통상적 개인 연습은 합리적인 기본 전제로 다루고, 미기재만으로 UNKNOWN을 만들지 않는다. 특정 자연환경·전용 시설·특수 장비와는 구분한다. 명시 소음/시간/예산 제한과 기존 UNKNOWN/FAIL 집행은 유지하며 후보명 allowlist로 우회하지 않는다. v18 코드 수정과 후속 TD-39의 실제 단일 평가 승인을 구별한다.

2026-09-18 후속 승인: 사용자가 **검색 없는 실제 32강 평가 1회** 재개를 승인했다. 아래 v17 수정의 단일 재평가만 실행하며 실패해도 재추첨하지 않는다. 이전 모든 사용액을 합산한 누적 $5 한도와 기존 호출 예약 정책·자동 충전 OFF를 유지한다. 이 승인은 추가 검색·반복 실험·공개 무인 운영으로 확대하지 않는다. **실행 결과: 첫 PLAN 호출이 401/expired_secret_key로 종료되어 승인한 시도는 사용했다.** 키 재발급·재개 전까지 추가 호출하지 않는다. $0.50 미확인 비용 예약은 임의로 지우지 않는다.

같은 날 추가 승인(TD-37): 사용자가 만료 키 교체·연결 후 같은 `hobby-solo/32`, v17 평가의 **추가 시도 1회**를 승인했고, 새 비공개 설정 저장 확인 후 실행을 마쳤다. **추가 승인도 사용 완료**다. 6회 응답은 HTTP 200이지만 최종 feasibility의 하모니카 연습 환경 UNKNOWN으로 `QUALITY_GATE_FAILED`, preview 없음, 사전 Repair 1회/검색 0회다. 추가 추정 $0.218444, 누적 장부 $4.4511778(미확인 예약 $0.50 포함), 잔여 $0.5488222. 새 승인 전 유료 재시도 금지. 검색·예약 완화·자동 충전 활성화·품질 승인으로 확대하지 않는다. 키는 채팅/로그/Git에 남기지 않는다.

- Node 24 LTS, Java 21. 루트에서 `npm ci`.
- 전체: `./scripts/verify.sh` — contract check, frontend test/type/build, backend test/bootJar/appJar.
- 프론트 상태/HTTP 단위 테스트: `npm run test:web` (Node 24 내장 test runner, 유료 호출 없음).
- 프론트: `npm run dev:web`; 백엔드: 루트 `docker compose up -d --wait postgres` 후 `cd backend && ./gradlew bootRun --args='--spring.profiles.active=dev'`.
- 프론트 착수: `docs/FRONTEND_HANDOFF.md`와 `docs/tickets/FE-001.md`. API 연결: dev 서버 실행 후 루트 `npm run api:smoke` (합성 생성 2회, 로컬 기록 생성). 인수인계 도구 단위 테스트: `npm run test:handoff`.
- 계약: `npm run contracts:generate`, `npm run contracts:check`.
- Java 범위 테스트: `cd backend && ./gradlew test --tests '*CandidateQualityGateTest'`.

## 제품 불변 조건

Candidate Quality 우선. N=8/16/32 정확, hard constraint/grounding 실패 시 노출 금지. Coverage Plan은 질문별 동적. 전체 preview와 전체 재생성 성공 1회, 시작 때 freeze. 시스템 Repair는 별도. 7초/timeout uniform random/Undo 없음/결승 동일. Timeout을 preference에 넣지 않는다. 공유는 같은 snapshot과 새 session, AI 재호출 없음.

## 구현 경계 / 완료 조건

- 현재 티켓 범위와 AC를 명시하고 구현/미구현을 구분한다. synthetic fixture와 실제 provider 실행을 섞지 않는다.
- frontend에는 provider key·history·내부 evidence를 넣지 않는다. 서버가 판정한 품질 통과 없이 preview DTO를 발급하지 않는다.
- 새 프레임워크/DB/큐/계정/실시간 그룹 기능은 현재 설계 밖이다. 문서로만 있는 provider/API를 구현됐다고 보고하지 않는다.
- behavior 변경은 관련 테스트 후 read-only Reviewer 독립 검토, 마지막 verify. Critical/High가 남으면 완료 아님. 동일 실패 fix/retest 최대 3회, review는 최초 포함 2회.
- PR에 변경한 AC/계약, 검증, 실제 provider 호출 유무, 남은 한계를 남긴다. 사용자 prompt/비밀키/개인 데이터를 로그/fixture/commit에 넣지 않는다.
- 서버 API는 `docs/BACKEND_DESIGN.md`의 Google 리소스 설계 적용/호환 예외와 DDD 경계를 따른다. 스타일 통일만을 이유로 공유 API를 깨지 않는다. 실제 엔진 개발과 서버 저장 구현의 접점은 CandidateEngine port이며 dev 대역을 운영 성공으로 사용하지 않는다.
