# /etc/worldcup/postgres.env, mode 600, root owner. Never send this file to app.
POSTGRES_PASSWORD=REPLACE_WITH_DIFFERENT_64_RANDOM_HEX_CHARACTERS
# Must equal app.env DATABASE_PASSWORD; the admin password must differ.
WORLDCUP_PASSWORD=REPLACE_WITH_64_RANDOM_HEX_CHARACTERS
