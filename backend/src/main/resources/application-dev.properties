# Explicit local-only synthetic engine. Never activate this profile in production.
spring.datasource.password=${DATABASE_PASSWORD:worldcup-local-only}
worldcup.public-origin=${PUBLIC_ORIGIN:http://127.0.0.1:5173}
worldcup.cookie-secure=false
