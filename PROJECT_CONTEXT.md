# 프로젝트 컨텍스트

갱신일: 2026-09-20.

최신 TD-59는 로컬 구현·검증 완료/미배포다. compact v6는 취미 외 작품·일반 선택의 단위와 중복 기준을 구분하고 명확한 짧은 요청의 불필요한 추가 질문을 막는다. 실제곡/SF영화16강과 저장/공유·현재차트 기대거절, 전체verify(Java469/프론트70)·최종리뷰2/2 Critical/High0. 실험누적$5.5110868/$6, 잔여.4889132<다음예약.50으로 추가 호출 중지. AWS 배포 인증이 만료됐으며 운영은 TD-58 그대로, SVG는 별도 로컬 시안이다. [엔진 기록](docs/CATALOG_ENGINE.md)과 [검증 기록](docs/VERIFICATION.md)을 따른다.

TD-58 제출 최종본 완료: 기획·디자인 대조 후 A 상하/현재 연출 유지·사진/이모지 제외. compact v4 활동 맥락/실제 장소 구분·GROUNDING_REQUIRED·접수 전429 복구를 구현했다. Java461/프론트70·HTTP175·16/32강184경기·독립 리뷰1/2 actionable0, 실제데이트16강1회2.987초/$0.012422와 현재사실 기대거절1회 통과. 실험누적$5.4446283/$6. 소스348a850 공통pipeline/AWS배포·공개파일일치·실제이전429입력복귀·기존snapshot5/hash·운영16건/$0.75603860 보존·백업73,121bytes 확인. DB migration/신규 자원/GitHub 권한 변경0. [제출본](docs/V1_SUBMISSION.md)이 최신이며 아래 수치는 각 단계 당시 기록이다.

최신 TD-57: 심사용 일일 제한0(일반 기본2)과 반복 배포 진입점을 구현·검증하고 공통 로컬 명령으로 `a08d5a3`를 기존 AWS에 배포했다. 동일actor DB16강3회 READY/AI0·HTTPS/asset바이트일치·snapshot5/hash보존·백업71,555bytes 확인. 단기 actor/IP5회/10분·운영 누적$5(현재15건/$0.74506260)·실험$6·장부/공유/게임은 유지한다. 최종 독립 리뷰2/2 Critical/High/actionable0. GitHub OIDC 역할·production 환경·main 통합은 별도 승인 대기다. 이전429 대기값이 저장된 기존 탭은 보존하며 즉시 시연은 새 시크릿 세션을 사용한다. 구성과 실제 적용 상태는 [배포 파이프라인](docs/DEPLOYMENT_PIPELINE.md)을 따른다.

최신 TD-56: HTML A 연출 재대조 후 내부 압박/VS/승패·마지막3초와 라운드 첫500ms 안내·상시 강수 경로를 구현해 소스 `521fbfe`를 기존 AWS에 배포했다. 7초/280ms·API·엔진·예산은 유지한다. 최종 verify·8강28경기 및16/32강184경기 합성 브라우저·독립 리뷰 Critical/High/actionable0. active 복원 포커스도 보완했다. 공개 파일의 검증본 바이트 일치·실제16강 직접7/timeout8·Champion저장·오류0, DB/snapshot/장부 보존·추가AI0 확인. 운영누적14건/$0.73137410이며 실험은 변화없다. [검증 기록](docs/VERIFICATION.md)이 증거다. [사진 설계](docs/CANDIDATE_VISUALS.md)는 검토 사진+SVG 대체를 제안할 뿐 수집/생성/구현은 하지 않았다.

최신 TD-55: **2026-09-20 1차 제출본 AWS 배포 완료.** 엔진·애니메이션 소스 `fcb6a5f`, catalog/30초·V4/seed64·HTTPS 확인. 전체 verify(Java452/프론트61)·독립 리뷰(Critical/High0)·합성 브라우저8/16/32를 통과한 동일 이미지다. 실제 DB preset0.767초/AI0, 조건부16강 preview5.357초/provider1회/$0.012404를 관측했다. 저장/공유/새세션·공개 브라우저15경기 timeout/Champion/공유·기존snapshot hash 보존·백업 완료. 임시 HTTP 점검기의 공유 본문 오류400은 남기고 DB preset으로 비용 없이 후속 확인했다. 운영 누적$0.697184/$5, 실험$5.4206158/$6·하루2회 유지. 32강 의미유사성과 일반 품질/SLA 미보장은 남는다. [1차 제출본](docs/V1_SUBMISSION.md)이 최신이며 아래 미완료/이전전략/숫자는 각 날짜의 역사 기록이다.

최신 TD-54: DB 편집 카탈로그 우선 + 단일 AI 선택/보충 경로의 로컬 구현·실험·검증을 완료했다. 일부 의미적 불완전성은 수용하되 미수행 독립 검토를 PASS로 위장하지 않는다. 64개 일반 seed/정확 preset 0회/일반 요청 최대 1회/구조 검사, 기존 계약·게임·예산 유지. 실제 8개 실험/유료 7회 추가 $0.09224, 누적 $5.3877598/$6. v2 취미 2.147초·식사6.368초는 단일 관측이며 의미 품질/SLA 보장이 아니다. Java451 실행·유료2 제외·전체 verify·최종 독립 리뷰 Critical/High/actionable0. 운영 기본값 staged/원격 Git 반영 없음. [설계·실험 현황](docs/CATALOG_ENGINE.md)이 최신 후보 엔진 방향이고 아래 TD-51 이하 독립 검토 정책은 보존된 기존 전략이다.

최신 TD-52: 사용자가 AWS 공개 배포와 별도 운영 AI 누적 총한도 $5를 승인했다. 기존 실험 $6과 분리하며 자동 충전 OFF·하루 2회·서버 비용 예약을 유지한다. AWS Free plan ACTIVE와 새 t3.small 적격성을 확인했고 기존 타 프로젝트 서버는 변경하지 않는다. 신규 결제/유료 플랜 전환/도메인 구매는 승인 범위가 아니다. 현재 HTTPS/VPC origin·신뢰 프록시·보존 DB·백업 설정을 구현하고 있으며 실제 URL/최종 이미지/호스트 smoke/복원 확인 전 공개 완료로 표기하지 않는다.

TD-51 당시 완료 범위는 엔진 기술 연결까지였고 전체 서비스 출시는 미완료였다. 공개 배포는 이후 TD-52/53, catalog 제출본 배포는 TD-55에서 완료했다. 32강 중복과 외부 사실/사람 평가의 한계는 해결된 것으로 바꾸지 않는다.

최신 설계 전환(TD-51): v23의 조건·계획·전체 후보 단일 생성 → 선택적 근거 확인 → 독립 전체 검토 → 필요 시 Repair 1회·재검토를 구현·검증했다. 사전 의도 검토/수정을 제거하고 실제 상세 후보에 수정 예산을 사용한다. 조건/단위/coverage·정상 후보 보존, 해석 FAIL/UNKNOWN 종료, 최종 품질 gate는 유지한다. 재사용은 최종 실제 증명만 보관하고 정책 v4로 구 증명의 재사용을 차단한다. 관련 262개·독립 리뷰 Critical/High/actionable 0·최종 verify(Java 358개/유료 2개 제외, 프론트 60개, HTTP 166개/9 schemas, 웹/두 JAR) PASS다.

v23 실제 hobby-solo/16도 별도 1회 PASS: 127.012초/4회 HTTP 200/Repair 1회/검색 0/$0.1380685. 테마 요리의 주방 접근 UNKNOWN을 보컬 연습으로 교체한 뒤 조건 32개·feasibility 16개와 전체 재검토를 통과했다. 같은 preview로 freeze→15경기 저장→Champion→동일 snapshot 공유·새 세션, 추가 AI 0회를 확인했고 실제 응답 14개/8 schemas도 계약 PASS다. 일반 주방을 과도하게 미확인으로 본 판정은 잔여 한계이며 후보 품질 보편성·32강 최신 실측·사람 평가·공개 배포를 완료했다고 주장하지 않는다. 아래 v22/이전 실패는 당시 기록으로 보존한다.

후속 v23/32의 변경 없는 첫 관측: 185.026초/4회 HTTP 200/$0.221852/검색 0/Repair 1회 뒤 QUALITY_GATE_FAILED다. c30의 외부 식물 접근 UNKNOWN만 교체한 뒤 전체 재검토가 그대로인 c7/8/9(연필 드로잉·색연필 컬러링·수채화)를 중복으로 거절했다. 최초 검토의 누락/판정 불일치가 남았으며 모든 응답은 출력·시간 제한 안에서 완료됐다. preview·완주 검증 없음, 재시도나 코드 변경 없음. 실제 테스트 1개 FAIL과 같은 코드의 16강 PASS/오프라인 회귀를 구별한다.

최신 실험 승인(TD-50): 이후 목적이 명확한 실제 평가는 매번 재질문 없이 순차 1건씩 진행할 수 있다. 누적 $6·검색 없음·자동 충전 OFF·기존 모델/Repair/예약 상한과 동일 실패 수정–검증 최대 3회는 유지한다. 현재 장부 $5.2955198/$6, 잔여 $0.7044802다. 잔액 처리 지연에 대비해 서버 한도를 유지하고 운영 예산/무한 반복 승인으로 확대하지 않는다. 아래 각 1회 승인 소진은 당시 기록이며 새 실행의 승인 기준은 TD-50이다.

최신 사용자 결정(TD-49): 평범하거나 호불호 있는 적격 후보를 허용하는 기준으로 1차 버전을 마무리한다. 16강 기본·8/16/32 지원을 유지하고, 주관적 매력 부족과 조건/중복/단위/지속성/실행 가능성/근거 결함을 분리했다. v22 공통 지침·재사용 정책, 관련 오프라인 207개와 독립 리뷰 Critical/High/actionable 0, 이후 전체 verify(Java 327개/유료 2개 제외·프론트 60개·HTTP 166개/9 schemas·웹/두 JAR)는 PASS다. 공개 계약/모델/Repair 한도는 불변이며 자동 축소는 없다.

후속 승인한 v22 실제 `hobby-solo/16` 1회는 **FAILED / QUALITY_GATE_FAILED**다. 47.849초/4회 HTTP 200/$0.063963/검색 0/사전 Repair 1회, 재검토에서 혼자 선호의 UNIT 해석 FAIL 및 그림 매체 중복으로 활동 15/16만 승인했다. 재미 부족 판정이 아니며 사전 검토 사이의 해석·중복 판정 불일치가 남았다. 상세 생성/preview/저장·공유 검증은 실행되지 않았다. 누적 장부 $4.9355993/$6(미확인 예약 $0.50 포함), 자동 충전 OFF·승인 소진·재시도 없음이다. 1차 버전 기준의 코드 반영과 실제 생성 안정화·운영 배포 미완료를 구별한다. 아래 v21/이전 실패와 금액은 당시 증거로 보존한다.

최신 실제 진단(TD-48): 사용자 승인 후 고정 v20 첫 ALLOCATE 입력을 v21 검토기에 그대로 전달했다. 1회 HTTP 200/17.382초/$0.021828, 비운동 제외 조건의 soft 오분류를 CONSTRAINTS FAIL로 검출해 진단 테스트는 PASS다. 새 계획/상세 후보/Repair/검색/preview는 없으며 전체 품질·중복 기준·최신 실제 완주는 미완료다. 누적 장부 $4.8716363/$6(미확인 예약 $0.50 포함), 자동 충전 OFF, 승인 소진. 아래 v21 미실행과 이전 비용은 당시 기록으로 보존한다. production/test 코드는 d85ab97 상태 그대로다.

최신 코드 보완(TD-47): v21은 직접적인 선택 범주 거부·상대적 선호·부정문의 분류 기준만 구체화한다. 생성/검토에 같은 지침을 전달하고 해석 FAIL/UNKNOWN은 기존 경로에서 종료한다. 재사용 정책 v3-engine-v21로 이전 기준 데이터와 분리했으며 모델/API/schema·Repair·예산은 유지했다. 관련 모의 테스트 163개 PASS, 독립 리뷰 Critical/High/actionable 0 뒤 전체 verify 통과(Java 313개 실행/유료 1개 제외·프론트 60개·HTTP 166개/9 schemas 등). 추가 유료 호출 0, 실제 의미 정확도·중복 해결·최신 실제 완주는 아직 미검증이다. 이전 x86 컨테이너는 v20 소스 검증이며 v21 runtime 이미지를 다시 빌드한 것은 아니다.

최신 실제 평가(TD-46): v20 `hobby-solo/32` 승인 1회 실행은 사전 Repair 뒤에도 활동 승인 30/32로 실패했다. 75.395초/4회 HTTP 200/$0.08744/검색 0, 상세 후보·preview 없음. 누적 장부 **$4.8498083/$6**(미확인 예약 $0.50 포함), 잔여 $1.1501917, 자동 충전 OFF이며 추가 유료 호출은 승인되지 않았다. 환경 기준은 사전 검토 PASS이나 최종 생성/feasibility 검증 전 종료라 해결을 확정하지 않는다. 아래 v20 미실행·이전 비용은 당시 기록이다.

이번 test-only 보완으로 실제 READY 결과를 재생성 없이 freeze→N−1 선택→Champion→같은 snapshot 공유·새 익명 세션까지 검증하는 공통 경로를 추가했다. 합성 8/16/32 HTTP 테스트 14개, 독립 리뷰 Critical/High/actionable 0, 이후 전체 verify(Java 309개/유료 1개 제외·프론트 60개·HTTP 166개/9 schemas 등)는 PASS다. 별도 실제 평가 1개는 READY 기대 불충족으로 FAIL이며 이번 실제 결과로는 완주 검사를 실행하지 못했다. 상세 실패 분석은 `docs/CANDIDATE_ENGINE.md`, 증거는 `docs/VERIFICATION.md` 최신 절이다. production 코드·계약·프롬프트는 이번 평가 작업에서 바꾸지 않았다.

최신 배포 비용 기준(TD-45): 사용자는 AWS 무료 혜택을 우선하며 Free plan 안에서 실제 결제 $0로 운영하는 방향이다. 일반 자원 요금과 크레딧 소비, 실제 결제를 구분한다. 월 $25 제안은 승인 예산/확정 견적이 아니며 구체 자원·사양은 미정이다. 유료 플랜 전환·계정 변경·신규 자원 생성은 하지 않았다. 무료 종료 시 접근 중단을 고려해야 하며, AWS 크레딧은 별도 AI API 예산을 대체하지 않는다.

최신 로컬 복구 확인: 사용자가 강제 종료·재실행을 승인한 뒤 Docker 엔진 응답을 복구했다. 기존 데이터·이미지·volume은 삭제하지 않았다. 로컬 ARM 이미지의 Java 파일 0바이트 문제는 별도 빌더 재수신 뒤에도 남았으나 동일 소스의 x86 이미지는 실제 시작·prod readiness·읽기 전용 smoke·패키지 운영자 CLI·합성 8강 공유까지 PASS다. 격리 DB의 별도 DB 복원과 대진/공유/장부 보존도 확인했다. Java 309개를 실제 재실행했고 유료 1개는 제외했다. 이는 운영 호스트 배포나 ARM 문제 해결이 아니다. 상세와 이미지 digest는 `docs/VERIFICATION.md` TD-45 후속 절을 따른다.

목적: 좋은 후보군과 빠른 A/B 선택으로 결정 비용을 줄이는 Dynamic AI World Cup. Candidate Quality가 우선이다.

## 최신 진행 — TD-42~44

사용자가 DB 검증 후 서비스 마감·배포를 승인했다. 현재 작업 트리는 `feat/engine/context-feasibility`이며 아래 과거의 'DB 제안만/배포 범위 밖/v19 미실행' 기록은 각 당시 상태다.

- v19 hobby-solo/32 승인 1회 실행은 최종 UNIT 해석 FAIL·베이킹 환경 UNKNOWN으로 실패했다. 6회/$0.238354/검색 0/preview 없음. 누적 장부 $4.7623683/$6(미확인 예약 $0.50 포함), 잔여 $1.2376317. 승인은 소진했고 추가 유료 호출은 없다.
- 사용자 정정(TD-44): 구매·준비·기초 학습으로 마련할 수 있는 장비/기술과 사용자가 통제하기 어려운 외부 환경을 구별한다. 오븐 소유 미기재만으로 베이킹을 거절하지 않되 명시한 예산·공간·소음 조건은 지킨다. 아파트 창밖의 새는 장비 구매로 확보되지 않는다. v20 생성/독립 검토 프롬프트에 반영했고 실제 모델 재평가·사람 승인은 아직 없다.
- 승인된 전체 후보 세트의 exact-context DB 우선 조회·원본 근거 재검증·미승인/개인 이력/grounding 제외·최대 7일 승인·폐기/감사 보존을 구현하고 합성/실제 격리 DB로 검증했다. 자동 학습·유사 문장 검색·pool 조합은 이번 범위가 아니다. 실제 승인된 세트는 아직 없어 운영 절감률을 주장하지 않는다.
- 초기 생성의 `CLARIFICATION_REQUIRED`에는 제품이 정한 보충 질문 1개와 명시적 재제출 경로를 연결했다. 모델이 만든 질문/대화 API가 아니며, 원래 요청+답변을 새 생성으로 접수하므로 일일 한도에 포함된다. 재생성 실패는 기존 preview를 유지한다.
- 통합 JAR/비루트 컨테이너, DB·웹 번들·엔진 설정 readiness, prod 오설정 시작 차단, 읽기 전용 release 점검을 준비 중이다. 배포는 위 TD-45의 AWS Free plan 기준으로 검토하며, 구체 자원/사양/도메인·프록시 선택은 미정이다. 신규 자원은 만들지 않았다.
- 최종 verify 통과: Java 309개 실행/유료 1개 제외, 프론트 60개, handoff 6개, release 13개, fixture 5개, HTTP 160개/9 schemas, 웹/두 JAR. 독립 Reviewer Critical/High/actionable 0. 당시 runtime 이미지는 저장소 I/O 오류로 실패했으나 TD-45 후속에서 x86 빌드·실행과 격리 DB 복원을 확인했다. ARM 로컬 계층 문제는 남았다. 상세는 `docs/VERIFICATION.md`다. 실제 AI 품질·공용 데이터 승인·최신 실제 AI와 프론트 완주·공개 운영 배포는 미완료다. 미리보기는 현재 계약대로 이름/태그이며 준비물 상세 표시의 충분성은 여전히 사람 평가 대상이다.

## 현재 구조와 상태

- 단일 저장소, `frontend` React/TypeScript/Vite, `backend` Java 21/Spring Boot 4.1.1. PostgreSQL 17/JDBC/Flyway 구현. 로컬 compose와 격리된 Testcontainers DB 검증을 사용한다.
- 큰 역할 A=플레이 경험, B=후보 품질과 서버. 제품 화면 작업과 서버 작업을 두 역할 안에서 계속 진행한다.
- `contracts/openapi.json` v1.0.0, 공유 fixture, 자동생성 TS, Java preview 직렬화 비교 테스트.
- CE-001 구현: fixed plan, candidate count/duplicate/unit/coverage, hard assessment, grounded evidence validity, independent semantic-review gate. 검증 통과 타입만 preview mapper에 넘긴다.
- 실제 HTTP 구현: health, 익명 생성 job/preview/전체 재생성, freeze/snapshot, 선택 batch, 공유/새 세션 API. cookie 소유권, idempotency, rate limit, worker lease/복구, 보존 정리 포함.
- 프론트는 `feat/product/split-deck-flow`에서 시안 4/A 상하 카드 기반 입력·preview·게임·Champion·공유와 실제 API 연결을 구현했다. 로컬 합성 서버로 전체 흐름을 검증했으며 후보 품질 승인·클라우드 배포는 별도다.
- `CandidateEngine` port 뒤에 CE-002의 OpenAI plan/생성/선택적 hosted search/독립 검토/Repair 최대 1회를 연결했다. `live`는 명시적 키/누적 예산이 필요하며 dev 혼용은 금지한다. dev 합성 후보는 개발용 표시, 기본 profile은 계속 실패 처리한다. 구현과 실제 검증 범위는 `docs/CANDIDATE_ENGINE.md`를 따른다.
- 직접 선택 history 최대 50건 중 관련 있는 선택만 계획에 반영하도록 했다. 이 호출 경계 구현과 실제 개인화 가중치 품질(CE-003)은 구별한다.

## 검증

CE-001 완료. `./scripts/verify.sh` 통과: 공용 fixture 5개와 생성 타입, TS/production build, Java 42개 테스트(실패/오류 0), bootJar. 독립 Reviewer 2회 중 최초 Medium 1/Low 1을 수정했고 최종 Critical 0 / High 0, 남은 finding 없음. 초기 화면은 360px/1280px에서 가로 넘침과 console error가 없음을 확인했다. 상세는 `docs/VERIFICATION.md`다.

GitHub: https://github.com/winkrj/dynamic-ai-world-cup (public, winkrj). 2026-09-14 사용자 승인으로 공개 전환했으며 인증 없이 저장소와 브랜치 조회를 확인했다. 링크만으로 clone하고 개발을 시작할 수 있다. 원본 쓰기 권한이 없는 참여자는 Fork → 개발 → PR을 사용한다. 구체적인 합류 순서는 README와 `docs/COLLABORATION.md`에 있다.

초기 검증 당시 시스템 기본 Node는 23이므로 별도로 받은 임시 Node 24.21.0을 사용했다. 팀원은 `.nvmrc`에 맞는 Node 24를 준비한다. 전역 Node 설정은 변경하지 않았다.

2026-09-14 API Goal 브랜치 `feat/server/backend-api`: 구현·최종 verify·독립 review 완료. Java 78개, 실제 HTTP 응답 102개/8개 schema, 기존 fixture 5개, 웹 build, bootJar 통과. 리뷰 1회차 Medium 2개를 수정했고 2회차 Critical 0 / High 0 / 남은 finding 0. 실제 dev 서버의 자동 worker·8강 기록·공유 replay도 확인했다. 아직 main에 병합한 상태는 아니며 실제 엔진/프론트 완성·배포 완료를 의미하지 않는다. 상세는 `docs/VERIFICATION.md`다.

## 다음 작업

TD-41/v19: unit의 뜻이 불명확했던 v18 경로를 보완해 비교 대상의 종류·수준과 기존 constraints/softPreferences를 명확히 분리한다. 공통 프롬프트/버전과 합성 회귀 검증만 변경하며 독립 해석 FAIL/UNKNOWN 차단은 유지한다. 실제 v19 평가 미실행·추가 유료 호출 미승인, DB 재사용은 제안 상태다. v18/사람 평가 실패를 소급 승인하지 않는다.

v19 구현 검증 완료: 관련 123개, 독립 리뷰 Critical/High/actionable 0, 최종 verify Java 249개 실행/유료 1개 제외·프론트 50개·handoff 6개·fixture 5개·HTTP 159개/8 schemas·웹/두 jar 통과. 추가 과금 0, 누적 장부 $4.5240143 유지. 실제 후보 품질 개선과 전체 Goal 완료를 의미하지 않는다.

최신 TD-39/40(2026-09-18): 실험 상한 $6과 v18 hobby-solo/32 1회 승인 후 실행 완료. 72.170초/2회 호출/$0.0728365, 사전 해석 UNIT FAIL로 `QUALITY_GATE_FAILED`, 상세 후보/preview·Repair·검색 없음. 현재 누적 장부 **$4.5240143 / $6**(사용량 추정 $4.0240143 + 이전 미확인 예약 $0.50), 잔여 $1.4759857. 자동 충전 OFF 유지, 추가 유료 재실행 승인 없음. 하모니카는 사전 검토만 승인됐으며 최종 후보 품질 검증은 이루어지지 않았다. 기존 사람 FAIL은 유지한다.

익명 브라우저별 하루 2회(서울 자정, 생성+재생성+접수 후 FAILED) 정책을 확정해 서버·UI에 반영했다. 기존 idempotency/rollback, 공유 AI 0회, burst/IP/전역 비용 guard는 유지한다. 신규 DB/HTTP 12개 포함 Java 244개 실행·프론트 50개·handoff 6개·fixture 5개·HTTP 159개/8 schemas·웹/두 jar 통과, 독립 리뷰 Critical/High/actionable 0. 360/1280px·긴 대기·수동 동일 key 재시도와 자동 POST 없음도 합성 브라우저에서 확인했다. 후보 DB 우선 재사용은 TECH_DESIGN 8절의 후속 설계 제안이며 아직 구현하지 않는다. 아래 검증·비용은 각 이전 시점의 보존 기록이다.

사용자 판단(TD-38): 소음 제한이 없는 요청의 하모니카 연습은 집에서 가능한 것으로 다룬다. v18은 일반 집 안 공간·통상적 연습과 특수 환경 접근을 공통 프롬프트에서 구별하며, 명시 조건/feasibility UNKNOWN 차단/Repair 상한을 유지한다. v17 실제 실패를 소급 PASS로 바꾸지 않는다. 추가 과금 없이 프롬프트 전달·기존 gate 회귀 테스트와 독립 리뷰로 확인했고, 이후 실제 단일 실행은 위 TD-39 결과와 구별한다.

v18 구현 검증: 관련 118개 통과, 독립 리뷰 Critical/High/actionable 0. 전체 verify Java 232개 실행(유료 1개 제외)·프론트 48개·handoff 6개·fixture 5개·HTTP 159개/8 schemas·웹/두 jar 통과. 추가 유료 호출 0회, 누적 장부 $4.4511778/잔여 $0.5488222 유지. 다음 실제 모델 평가와 사람 품질 승인 없이 전체 Goal 완료로 판단하지 않는다.

2026-09-18 제품 통합 검증: 프론트 48개, Java 192개 실행(유료 1개 제외), handoff 6개, fixture 5개·HTTP 159개/8 schemas, 웹/기본 jar/app jar 통과. 브라우저에서 360px 8/16/32의 7/15/31개 선택 저장 및 같은 snapshot·새 세션 공유 replay를 검증했다. 실패 후 새로고침 복구·느린 이미지 2초 fallback·긴 이름·360/1280·키보드 선택도 별도 합성 응답으로 통과했다. 독립 리뷰 최초 High 1/Medium 1을 수정하고 재리뷰 Critical/High/actionable 0. 추가 유료 호출 0. 상세/잔여 한계는 `docs/INTEGRATION_SPEC.md` 10절이다.

시안 4/A 상하 카드 제품 통합은 위 검증 범위까지 완료했다. 사용자가 엔진 수정 보류를 해제해 현재는 `feat/engine/context-feasibility`에서 **필수 환경·접근 전제의 후보별 검토**를 보완한다. 기존 frontend/API는 유지하며 추가 유료 호출 없이 검사 구조·회귀 테스트를 먼저 구현한다. 실제 모델 품질·미리보기 정보 충분성·2명 사람 평가·배포 승인은 별도다. 상세는 `docs/CANDIDATE_ENGINE.md` v17 절, 역할 경계는 AGENTS, 제품 기준은 [제품 통합 Spec](docs/INTEGRATION_SPEC.md)이다. 아래는 보존된 엔진 체크포인트다.

2026-09-18 CE-002/v17 구현 검증: 후보별 feasibility 판정을 같은 독립 검토 호출에 추가했다. 누락·UNKNOWN·FAIL은 차단하고 기존 Repair 합계 1회를 유지한다. 관련 116개 테스트 통과, 독립 리뷰 Critical/High/actionable 0, 전체 verify Java 230개 실행(유료 1개 제외)·프론트 48개·handoff 6개·fixture 5개·HTTP 159개/8 schemas·웹/두 jar 통과. 추가 유료 호출 0이며 실제 판단 개선율은 미검증이다.

v17 실제 재평가는 이후 사용자 승인으로 **1회 시도**했지만 첫 PLAN이 401/expired_secret_key로 종료됐다. 실제 통합 테스트 1개 실패, 후보/preview·검색·Repair 없음. 키 재발급 전 추가 호출하지 않는다. 자동 충전 OFF를 결제 화면에서 재확인했다. 사용량 기반 누적 $3.7327338에 미확인 비용 예약 $0.50을 더해 예산 장부 **$4.2327338 / 승인 $5**, 잔여 $0.7672662이며 실제 $0.50 청구 주장과 구별한다. 원시 기록은 gitignored, 코드 오프라인 검증과 품질 미검증 상태는 유지한다.

TD-37 추가 승인 후 새 비공개 설정 저장을 확인하고 같은 v17 `hobby-solo/32`를 **1회 재시도 완료**했다. 인증은 복구돼 6회 모두 HTTP 200, 32개 상세 후보/조건 판정 64개/feasibility 32개를 받았다. 최종 feasibility는 PASS 31/UNKNOWN 1(하모니카 연습 환경 미확인)이므로 `QUALITY_GATE_FAILED`, preview 없음. 사전 뜨개질 중복 교체에 Repair 1회를 이미 사용해 추가 Repair는 없었다. 180.132초/$0.218444, 검색 0회. 실제 테스트 1개 실패와 HTTP 상태 응답 2개 schema 통과를 구분한다. 현재 사용량 기반 누적 $3.9511778 + 이전 미확인 예약 $0.50 = **장부 $4.4511778 / $5**, 잔여 $0.5488222. 추가 승인도 사용했으므로 새 승인 전 유료 재시도 금지. 사전 검토가 최종 환경 문제를 놓친 점, 공예·퍼즐 편중, 사람 평가/외부 사실 평가 미완료는 남아 있으며 인증 복구를 제품 품질 통과로 보지 않는다.

2026-09-16 CE-002/v16: 실행마다 PLAN schema 속성 순서가 달라 후보를 조건보다 먼저 쓰던 구조적 불일치를 고쳤다. 조건·선호→비교 단위/성격·grounding→결정→coverage 순서를 고정한다. 새 필드·모델 호출 없이 기존 Context/Constraint→Unit→Coverage 설계를 반영한다. v15의 단일 선택 기준, 조건별 근거/독립 검토, 사전·상세 Repair **합계 최대 1회**, 시간/비용 한도와 공개 API는 그대로다.

v16 solo/32 비교 1회는 **READY·preview 32개**, 217.268초, 6회 호출/사전 Repair 1회, $0.249958. 실제 schema와 출력 순서가 같았고 비운동 제외·월 예산이 별도 필수 조건에 들어가 32×2=64개 독립 assessment를 모두 받았다. 순서 고정과 이번 실행에서의 제외 조건 보존은 확인했지만 인과관계나 보편적 의미 정확도 개선율은 확정하지 않는다. 초기 35개 제안 중 장르/재료 중복 등 14개가 탈락해 교체됐고 재검토 후 32개를 승인했다. 최종 감상·필사·퍼즐 세분화와 장비 전제 판단의 일관성은 사람 기준 대조가 남아 있어 품질 완료가 아니다.

v15의 미실행 일반 취미 3세트를 각각 한 번 확인했다. **solo/8 READY(62.728초/$0.072544), solo/32 READY(189.510초/$0.2141135)**, home/32는 해석·후보 문제로 FAILED(114.110초/$0.1066105)였다. solo/32에서 사전 활동 교체 1회 후 재검토→실제 preview까지 연결됐다. 이전 home/8·16, social/32, solo/16의 READY도 보존하며 버전별 결과는 `docs/CANDIDATE_ENGINE.md`에서 확인한다.

**자동 READY는 품질 최종 승인이 아니다.** solo/32는 스케치/색칠, 독서/주제 탐구, 퍼즐 분할을 통과시켰고 ‘운동은 싫어’를 필수 조건 대신 softPreferences와 unit에만 남긴 해석도 PASS였다. 실제 후보는 비운동 활동이지만 제외 조건의 독립 assessment 누락을 정확히 잡았다는 증거는 아니다. home/32는 원문 30분을 계획이 ‘30분 내외’로 표현한 문제를 두 번째 검토에서야 거절했고 상세 생성/preview 없이 끝났다. 후보 경계와 해석 판정 일관성이 남아 있어 Goal은 미완료다. 같은 사례를 통과할 때까지 재추첨하지 않는다.

서울 접근성 사례는 v14에서도 근거 부족으로 FAILED였다. 후보 선정 시점의 검증 가능성과 유효한 대체 활동 확보가 남아 있으며 이번 v15에서는 유료 검색을 하지 않았다.

v16 최종 `verify`: Java **171개 실행/실패·오류 0(유료 1개 제외)**, handoff 6개, fixture 5개, 실제 HTTP 102개/8개 schema, 웹 build/bootJar 통과. 집중 테스트 120개 후 독립 리뷰 1회 Critical/High/actionable 0, 이후 코드 변경 없이 전체 검증했다. 별도 live HTTP 69개도 schema 적합(실패 응답 포함). 누적 실험 추정 **$3.7327338 / 승인 $5**, 제공자 124회, 미확인 비용 예약 없음, 자동 충전 OFF 유지. seed **11/18세트**의 첫 실행 READY 5/FAILED 6은 그대로이며 일반 취미 9세트는 모두 최초 실행했고 외부 사실 7세트와 2명 사람 평가는 남아 있다. **잔여 $1.2672662 < 검색 예약 $2.00**여서 추가 유료 검색은 보류한다. 한도/예약을 임의로 완화하지 않으며 배포·프론트 완주 완료를 뜻하지 않는다.

- A/B 제품 통합: 검증한 React 흐름·dev 서버 연결을 유지한다. 예전 FE-001 시작 안내와 별도 팀원의 clone 절차는 역사적 인수인계 자료다.
- B 엔진 후속: 수정 보류 해제에 따라 필수 접근 전제의 누락을 보완한다. 외부 사실의 출처/적용 범위, 큰 후보군의 분할·filler 및 사람 평가는 여전히 남아 있다. Terra는 개발 기본값이며 최종 모델 선정/2명 품질 평가 완료가 아니다. 유료 실험은 이번 코드 검증에 포함하지 않는다.
- OpenAI API는 별도 사용자 결제/키와 최신 TD-39 누적 실험 예산 $6 범위에서 사용하며 승인된 1회는 사용 완료다. 자동 충전 OFF, 추가 호출 승인 없음. 저장소에 키나 실제 비공개 평가 원문을 추가하지 않는다. 원래 PRD/Design/AC/Decision 첨부 원문을 확보하면 복원본과 대조한다.
- GitHub collaborator 초대는 역할별 실제 계정이 정해진 뒤 한다. 현 단계에서 팀원 초대/branch protection을 완료했다고 주장하지 않는다.
- 외부 공유 개발 API/Swagger UI와 공개 배포 주소는 아직 없다. 시안 4 기반 제품 화면은 로컬에서 구현·검증했다. OpenAPI JSON·생성 TS 타입·fixture가 API 전달물이며, `npm run api:smoke`는 로컬 dev API의 생성부터 공유 replay까지 연결을 확인한다. 실제 AI 품질이나 프론트 완주 UI 테스트를 대신하지 않는다.

핵심 동시성/게임 정책과 기술 선택은 `TECH_DESIGN.md`, 결정 변경은 `DECISIONS.md`, source 한계는 `docs/SOURCE_PROVENANCE.md`에 남긴다.
