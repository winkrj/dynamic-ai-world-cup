# TD-54 — DB 우선, 한 번의 선택/보충

최신 작업(TD-59, 2026-09-20): compact prompt `ce003-v6-clear-subject` 로컬 구현·검증 완료이며 운영은 아직 TD-58 v4다. AWS 배포 인증 만료를 확인했고 운영 자원/설정을 변경하지 않았다. 활동 외 일반 선택 지원은 아래 기준을 따른다. 이전 관측과 실패는 보존하며 실제 배포 여부는 [제출 현황](V1_SUBMISSION.md)과 구분한다.

## TD-59 — 비교 대상에 따른 일반 추천

운영에서 짧은 K-pop 추천 입력이 `UNSUPPORTED_REQUEST`로 종료됐다. 기존 프롬프트의 활동 중심 지원/중복 기준이 작품 비교와 충돌할 여지가 있었으며, 모델의 내부 거절 이유를 관측한 것으로 주장하지 않는다. PRD는 원래 취미만을 입력 범위로 제한하지 않는다.

| 요청 | 후보/중복 단위 | 사실 확인 경계 |
| --- | --- | --- |
| 취미·데이트 | 핵심 활동; 도구/재료만 바꾼 세분화 금지 | 기존 명시 조건·외부 환경 규칙 유지 |
| 노래·영화·책·게임 | 개별 작품; 노래는 곡명+가수, 책은 제목+저자 | 알려진 안정적 작품만. 최신 차트/상영/스트리밍 제공 여부는 지원하지 않음 |
| 음식·선물 종류·여행지·창작 아이디어 | 요청의 비교 수준에 따른 개별 선택 | 가게/구매할 상품 모델·현재 가격·예약 등은 지원하지 않음. 실제 대상과 창작 구별 |

예시는 허용 목록이 아니다. 먼저 unit을 정하고 catalog에 알맞은 후보가 없으면 additions로 전부 구성한다. family는 동일 선택의 중복 키이며 작품에서는 장르/작가/가수와 다르다. 같은 작품의 번역명·리마스터로 수를 채우지 않는다. 서버의 정규화 이름/family/N 검사는 그대로이며 모델이 서로 다른 키를 만들어내는 의미 중복은 여전히 한계다.

새 검색 API·추가 모델 호출·Repair·DB seed/migration·공개 DTO 변경은 없다. 0/1회, 기존 Terra/30초/4096 출력 토큰, 실험 누적$6·운영 누적$5와 .50 예약을 유지한다. 제목/창작자 정확성은 검색 검증이 아닌 best-effort이며 모든 주제의 성공/사실 정확도를 보장하지 않는다. 사용자 원문을 fixture에 복사하지 않고 별도 합성 곡/영화/책/최신 순위 사례를 추가했다.

프롬프트의 범주별 규칙/예시를 일치시키는 수정은 [OpenAI 공식 prompting 안내](https://developers.openai.com/api/docs/guides/prompt-engineering)를 참고했다. 모델/SDK 변경은 하지 않는다.

v5 첫 곡16강 시험은 1회/1.989초/$0.012903으로 CLARIFICATION_REQUIRED였다. 요청의 대상과 선택적 취향 조건을 혼동할 여지가 남아 v6에서 ‘무엇을 고를지조차 정할 수 없는 경우만 추가 질문’으로 명확히 했다. 같은 합성 입력의 수정–재검증 1회는 READY16곡·저장·공유, provider1회/9.064초/$0.020048이었다(HTTP 후속 포함9.651초). 같은 가수의 다른 곡이 유지됐고 검색/Repair0이다. 이전 실패를 지우거나 보편적 품질 통과로 세지 않는다.

후속 v6 SF영화16강도 READY·15경기 저장·동일 snapshot 공유까지 통과했다(1회/8.900초/$0.019950, HTTP 후속 포함9.408초). 최신 음원 순위8강은 기대한 GROUNDING_REQUIRED로 종료했다(1회/1.927초/$0.0135575, 전체2.192초). 제목/창작자 외부 대조는 하지 않았으며 책·여행지·작품32강은 유료 미검증이다. 일반 분야 전체의 품질 통과나 p95/SLA로 설명하지 않는다.

원시 기록은 gitignored `reports/local/live-engine/2026-09-20T13-00-34.160618Z-size16`(v5실패), `2026-09-20T13-02-37.882080Z-size16`(v6곡), `2026-09-20T13-03-51.947950Z-size16`(v6영화), `2026-09-20T13-04-47.296206Z-size8`(v6차트거절)에 보존했다. 실패 포함4회/$0.0664585 추가, 실험 장부167행+기존 비용 합계 **$5.5110868/$6**(기존 미확인.50포함). 잔여$0.4889132는 다음 .50 예약보다 작으므로 추가 유료 호출을 중지했다. 운영 장부/상한을 바꾸지 않았다.

최종 전체verify: Java469실행/유료2제외/실패0, 프론트70, HTTP175/9schemas, 웹·bootJar·appJar 통과. 독립 read-only 리뷰2/2 Critical0/High0/actionable0. 수정–실제 재검증은 같은 곡 사례1회로 제한했고 이후 코드 변경 없이 대조했다. 합성 오류/복구 브라우저8사례×360/1280, keyboard/reduced-motion/reload PASS·오류0. 운영 배포·SVG 적용은 하지 않았다.

## TD-58 제출 경계 검증

일반 규칙과 대비 예시를 프롬프트에 추가했다. 특정 도시의 키워드를 서버에서 성공 처리하지 않는다. 예시로 요청 유형을 분리하는 방법은 [OpenAI 프롬프트 지침](https://developers.openai.com/api/docs/guides/prompt-engineering)의 few-shot 방식과 맞추되 모델·단일 호출·검색 없음·토큰/시간/비용 예약은 그대로다. 과거 운영 실패는 원래 모델 decision을 저장하지 않아 grounding 때문이라고 확정할 수 없으며, 이 변경은 과잉 분류 가능성과 모호한 실패 안내를 함께 줄인다.

| 고정 합성 사례 | 실제 결과 | provider / 전체 HTTP 경로 | 비용 |
| --- | --- | --- | --- |
| `catalog-date-context`: 서울 평일 데이트,16강 | READY, 후보16·freeze·15결정 저장·동일 공유/새 세션 확인. 일반 활동16개이며 실명 업체/현재 가격 보증 없음 | 1회 / 2.987초 · 전체3.542초 | $0.0124220 |
| `catalog-facts`: 실제 영업 식당 이름·현재 가격,8강 | FAILED/GROUNDING_REQUIRED, preview 없음. 기대한 거절 경계 검사 통과 | 1회 / 2.430초 · 전체2.660초 | $0.0115905 |

추가 $0.0240125, 실험 누적 **$5.4446283/$6**, 잔여$0.5553717(기존 미확인 .50 포함). 각각 목적별1회·자동 재시도/검색/Repair0. 첫 facts 검사 명령은 Gradle UP-TO-DATE로 실행되지 않아 관측으로 세지 않았고, 이후 실제 실행1회를 확인했다. 고정 입력의 성공/거절 관측이지 전체 정확도·평균/p95·사람 품질 PASS가 아니다. 사진과 후보 시각 자산은 추가하지 않았다.

원본은 gitignored `reports/local/live-engine/2026-09-20T10-40-13.287445Z-size16`, `2026-09-20T10-43-09.586030Z-size8`에 보존한다. 과거 R 평가 FAIL·32강 의미유사성·일반 입력의 제약 해석 한계는 남는다.

## 결정과 목표

2026-09-19 사용자는 최초 생성 30초도 길며, 16개 전부가 완벽할 때까지 검증하는 것보다 프롬프트로 한 번에 충분히 좋은 후보를 만들고 일부 어색한 선택을 수용하겠다고 결정했다. 미리 DB에 후보를 준비하고 부족분만 보충하는 방향으로 실험한다. 모든 입력에서 완벽한 의미 정확도를 보장할 수 없다는 한계를 인정하되, 알려진 조건 위반을 의도적으로 허용한다는 뜻은 아니다.

이번 실험 목표(달성 수치 아님): 정확한 편집 preset은 로컬 worker 1초 이내/AI 0회, 새로운 16강은 단일 AI 호출 10초 안팎·$0.05 이하를 지향한다. 지연/비용 목표를 못 맞췄다면 그대로 기록한다. 소수 샘플로 p95·일반 성공률을 주장하지 않는다. 입력 다양성·명시 조건·의미 중복·비교 단위를 함께 관찰하고, 매력 차이 자체는 실패로 삼지 않는다.

기존 사례: 운영 16강은 212.375초, 6개 Responses 호출, 장부 $0.53859000였다. 첫 생성 30.794초/$0.03544550, 사실 확인 두 단계는 합계 $0.34822300다. 이는 특정 검색/수정 사례이지 16강 평균 요금이 아니다. 검색 집계의 fallback은 web_search_call action 종류를 분리하지 않으므로 장부와 최종 청구를 구별한다.

## 요청 경로

1. 기존의 명시 승인 complete-set 재사용을 먼저 확인한다. 구 certificate는 기존 정책으로만 검증한다.
2. 새 카탈로그의 **정확한 preset**이고 개인 선택 이력이 없으면 편집 후보를 DB에서 선택한다. N보다 부족하거나 재생성에서 같은 전체 세트밖에 없으면 다음 단계로 간다.
3. 그 외 요청은 SQL로 최대 64개 후보를 가져온다. 키워드는 검색 우선순위일 뿐 조건 충족 판정이 아니다. 모델 한 번이 원문 조건과 선택 이력을 고려해 DB ID를 고르고 부족한 후보만 추가한다. 후보를 새로 만들 필요가 없다면 ID만 반환한다.
4. 서버가 후보를 합치고 N·알려진 ID·이름/family 중복·단위·필드·구조를 검사한다. 실패하면 종료하며 reviewer/Repair/검색/느린 엔진을 자동 호출하지 않는다. worker crash 뒤 attempt=2는 DB 전용 복구만 허용하고 유료 보충은 거절하여 미확인 비용을 다시 쓰지 않는다.
5. 통과한 preview 이후에는 기존 freeze/게임/저장/공유를 사용한다. snapshot은 카탈로그 참조가 아니라 당시 display copy다.

`CANDIDATE_ENGINE_STRATEGY=catalog`로 명시 선택한다. 기본값 `staged`를 보존해 이번 코드 작성만으로 운영 정책을 바꾸지 않는다. catalog deadline 기본 30초(허용 1~60초), reasoning `none`, output 상한 4096, tools=[], 예약 $0.50이다. 상한은 사용 목표나 보장 응답 시간이 아니다. 같은 모델 Terra를 유지하여 구조 변화 효과부터 관찰한다.

## 품질·신뢰 경계

| 항목 | catalog 정책 | 기존 staged 정책 |
| --- | --- | --- |
| 후보 적합성·명시 조건·의미 중복 | 프롬프트/편집 seed 기반 best-effort; 일부 오판 가능 | 별도 독립 모델 검토를 집행; 검토 정확도도 완벽하지 않음 |
| 개수·ID·정규화 이름·family·필드 | 서버 검사; 실패를 가짜 PASS로 덮지 않음 | 기존 서버 검사 |
| Coverage | 한 번에 다양하게 구성하도록 지시, 결과 카테고리 개수 집계 | 고정 계획 quota와 독립 검토 |
| 현재 장소·가격·영업 등 사실 | GROUNDING_REQUIRED → 기존 보충 질문 오류, 사실 확인 완료 주장 없음 | 필요한 검색·유효한 근거 필요 |
| 증명 | FAST_BEST_EFFORT / fast-best-effort-v1, certificate=null | 기존 독립 증거와 certificate |
| 반복 | 요청당 AI 0~1회, 자동 Repair 없음 | 기존 bounded Repair 최대 1회 |

새 경로는 AC-03/04/06/07/20의 독립 계획·판정 집행 방식을 명시적으로 바꾼 실험이다. 생성자가 constraintSources로 원문을 발췌했다고 이해·준수를 증명하지 않는다. 같은 family 검사는 알려진 ID 일치에만 강하며 새 표현의 의미 중복 전체를 보장하지 않는다. 실명 장소 요청을 일반 활동으로 몰래 바꾸지 않는다. 일반 데이트 아이디어는 실제 장소 예약·영업 보증과 다르다. 최신 사실이 필요한 요청의 빠른 성공은 이번 완료 범위가 아니다.

## DB 준비와 재사용

V4에 일반 취미 32개/데이트 활동 16개/식사 메뉴 16개, 총 64개 AI 편집 seed를 넣는다. `AI_CURATED_EDITORIAL_V1`, `human_rated=false`, time-independent와 active를 명시한다. 사람 평가 통과·독립 모델 검토 통과라고 표시하지 않는다. 원래 의미가 같은 그림/악기/공예를 매체만 바꿔 개수를 채우지 않는다.

0회 preset은 `취미 추천해줘`, `데이트 활동 추천해줘`, `저녁 메뉴 추천해줘` 세 문구의 NFKC/공백 정규화 일치와 ko-KR/Asia/Seoul에만 적용한다. 조건이나 문장부호를 삭제해 강제로 적중시키지 않는다. 의미가 비슷한 문장은 일반 1회 선택 경로다. date/dinner 32강은 부족분을 한 번 보충한다.

개인 원문과 새 응답을 공용 catalog로 자동 저장하지 않는다. 새 생성 결과는 기존 private draft로 저장되지만 구 승인 complete-set certificate는 만들지 않는다. 운영자 검토·추가 seed·조건별 속성/검색 범위 확장은 후속 작업이다. 벡터 DB/임베딩/학습 작업은 도입하지 않는다.

## 실험·예산·출시 판정

`evals/catalog-cases.json`의 공개 합성 사례를 사용한다. 기존 hobby-solo 원문, 일반 데이트, 고기/매운맛 제외 저녁, 카탈로그 밖 창작, 최신 사실 거절을 구별한다. 최초 결과와 실패를 보존하고 선택 ID/보충 후보/latency/토큰/장부 비용을 기록한다. 사람 최종 품질 승인을 자동 추론하지 않는다.

일반 test/verify는 CANDIDATE_LIVE_TEST=false. 유료 실행은 같은 기존 LiveEngineHttpTest를 사용하고 CANDIDATE_ENGINE_STRATEGY=catalog, CANDIDATE_LIVE_CASE=catalog-*, 해당 강수와 실험 **남은 누적 예산**만 자식 프로세스에 주입한다. 운영 $5와 실험 $6은 별개이며 섞거나 DB 초기화로 한도를 늘리지 않는다. 검색 없음/자동 충전 OFF/.50 미확인 예약 유지. 동일 실패 수정–재검증 최대 3회, 독립 코드 리뷰 최대 2회.

배포 전 관련 단위·실제 격리 DB·HTTP 재생성/완주/공유·기존 strict/reuse 회귀·전체 verify와 독립 코드 리뷰가 필요하다. 코드 통과와 모델 의미 품질 관찰, 실제 운영 반영은 각각 구별한다.

## 실행 결과

2026-09-19, 동일 Terra/검색 없음/reasoning none. 실제 API를 로컬 Spring·격리 PostgreSQL에서 호출했다. 서버를 시작하는 시간은 아래 지연에 포함하지 않는다. `연결 전체`는 HTTP 접수→worker 실행→preview→N−1 경기 저장→공유/새 세션 검사 합계이며 브라우저/운영 네트워크/대기열 SLA가 아니다. 거절 사례는 terminal 조회까지만 측정했다.

| 버전 / 사례 | 강수 | DB / 새 후보 | AI 호출 시간 | 연결 전체 | 비용(USD) | 관찰 |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| v1 취미 | 16 | 15 / 1 | 4.923초 | 5.324초 | 0.0117565 | READY·저장/공유 통과. 캘리그라피/그림 경계는 취향에 따라 유사하게 느낄 수 있음 |
| v1 일반 데이트 | 16 | 16 / 0 | 2.317초 | 2.728초 | 0.0113775 | READY·저장/공유 통과. 실명 장소/가격 확인 사례 아님 |
| v1 고기/매운맛 제외 식사 | 16 | 8 / 8 | 4.086초 | 4.510초 | 0.0141705 | 기술 흐름은 READY지만 **내용 검토 보류**: 일반 쌀국수·샤부샤부 등에 고기 없는 변형을 숨겨 가정한 문제 |
| v1 DB 밖 판타지 능력 | 16 | 0 / 16 | 5.988초 | 6.415초 | 0.0159450 | READY·저장/공유 통과. 전부 생성해도 긴 설명/평가 없이 517 출력 토큰 |
| 편집 취미 preset | 16 | 16 / 0 | 호출 없음 | 0.391초 | 0 | provider 장부 0건, 저장/공유 통과 |
| v2 식사 재비교 | 16 | 0 / 16 | 6.368초 | 7.339초 | 0.0167080 | 숨은 변형 금지 후 채소 비빔밥·채소 쌀국수(채수) 등. 첫 결과를 소급 승인하지 않음 |
| v2 최신 식당/가격 | 8 | 0 / 0 | 2.455초 | 2.711초 | 0.0104460 | 기대대로 FAILED/CLARIFICATION_REQUIRED, preview 없음. 사실을 확인한 성공이 아니라 안전한 거절 |
| v2 취미 회귀 | 16 | 16 / 0 | 2.147초 | 2.657초 | 0.0118365 | READY·저장/공유 통과, 144 출력/0 reasoning 토큰 |

총 **8개 실험·유료 provider 7회·추가 $0.0922400**, 검색 0. 이전 누적 $5.2955198(미확인 $0.50 포함)에서 **$5.3877598/$6**, 잔여 **$0.6122402**다. 운영 $5 장부와 섞지 않았다. 각 실행 직전 전체 실험 ledger를 다시 합산하고 .50 예약 가능 여부를 확인했으며 자동 충전 설정은 변경하지 않았다. 이 금액은 usage 기반 장부이지 최종 청구서가 아니다.

프롬프트는 v1에서 한 번만 수정했다. v2 `ce003-v2-visible-choice`는 **DB 후보명 그대로 조건에 맞지 않으면 선택하지 않고, 조건이 드러나는 새 이름을 보충**하도록 한다. v1 식사 실패를 입력 변경/정답 seed 삽입으로 숨기지 않았고 같은 요청으로 재비교했다. 이 한 번의 개선이 모든 식이 조건/육수/조리법을 완벽하게 검증한다는 뜻은 아니다. 일반 후보 기반 DB이므로 조건이 까다로운 식사에서 적중률이 0으로 떨어지는 한계도 확인했다.

최종 취미 예시: 독서, 그림 그리기, 베이킹, 외국어 학습, 뜨개질, 디지털 작곡, 실내 식물 가꾸기, 창작 글쓰기, 마술 연습, 코딩, 영상 편집, 레고 조립, 핸드드립 커피, 직소 퍼즐, 종이접기, 명상. 매력/장비별 실제 비용/소규모 유사성은 보편적 승인으로 주장하지 않는다. 최종 식사 예시 중 두부조림 등은 조리법에 따라 매울 수 있어 명시 조건 의미 정확도의 잔여 한계로 남긴다.

원본은 gitignored `reports/local/live-engine/2026-09-19T03-*`의 catalog 사례 8개에 보존한다(원본 요청·응답·ledger·HTTP/playback). 과거 데이터는 수정하지 않았다. 일반 단위·격리 DB·HTTP·전체 verify와 독립 코드 검토 결과는 `docs/VERIFICATION.md`를 따른다. 운영 서버/원격 Git에는 이번 변경을 적용하지 않았다.

근거: [OpenAI 지연 최적화](https://developers.openai.com/api/docs/guides/latency-optimization)는 출력량·요청 수 감소와 LLM을 사용하지 않는 경로를 제안한다. [Terra 모델 문서](https://developers.openai.com/api/docs/models/gpt-5.6-terra)에서 none reasoning 지원을 확인했다. 이번 결과는 DB ID 재사용·출력 축소·reasoning 설정·온라인 검토 제거를 함께 바꾼 복합 실험이며 프롬프트 문구만의 인과 효과로 분리하지 않는다.

## 제출 전 32강 관측 — TD-55

같은 `catalog-hobby` 입력의32강을 처음 확인한 뒤, 핵심 경험 묶음/외부 환경 접근 지시를 한 번 보완하고 같은 입력으로 한 번만 비교했다. 모델·schema·최대호출·timeout·seed는 바꾸지 않았다. 두 시도 모두 API→preview→31경기 저장→공유/새 세션 연결은 PASS지만 이는 의미 품질 PASS가 아니다.

| 버전 | AI 호출 | 연결 전체 | 장부 비용(USD) | 내용 판정 |
| --- | ---: | ---: | ---: | --- |
| v2 | 5.823초 | 6.268초 | 0.0161035 | 보류: 그림/픽셀아트, 종이접기/페이퍼크래프트 등 유사 활동과 새 관찰 일지/구름 관찰의 환경 전제 |
| v3 | 2.827초 | 3.318초 | 0.0167525 | 환경 관찰 후보는 사라졌지만 그림/디지털 일러스트, 십자수/자수, 종이접기/페이퍼크래프트 등 유사 후보가 여전히 남음 |

v3 지시는 상위/하위 활동의 동시 선택 금지, 같은 핵심 경험의 family 재사용, 관찰 대상을 확보할 환경이 확인된 경우에만 선택하도록 한다. **지시 전달 테스트는 의미 중복 차단 증명이 아니다.** 이번 비교만으로 문제가 해결됐다고 주장하지 않으며 추가 유료 재추첨은 하지 않았다. 32강은 선택 폭을 원하는 사용자가 비슷한 후보를 감안해 미리보기에서 판단하는 경로로 유지하고, 기본 추천은16강이다. 같은 v3의 운영16강 실측은 배포 후 별도 확인 대상이다.

이번 두 요청 추가비용 $0.0328560, 실험 전체 누적 **$5.4206158/$6**(미확인예약$0.50 포함), 잔여$0.5793842. 각1호출/검색0/자동Repair0. 원본은 `reports/local/live-engine/2026-09-19T16-24-38.233875Z-size32`와 `2026-09-19T16-52-39.612148Z-size32`에 보존했다. 저렴한 단일 호출과 주관적 불완전성 허용을 선택한 제출 버전이며, 사용자 기준의 모든 중복이 해결된 완벽한32강 모델로 소개하지 않는다.

## 같은 v3의 운영16강 관측 — 2026-09-20

검증된 소스를 기존 AWS에 적용한 뒤, 동일 공개 합성 고민(혼자/비운동/월10만원이하 취미)의16강을 **한 번만** 호출했다. HTTP접수→전체preview5.357초, provider4.455초/1회, 입력4271·출력144tokens, 검색0/Repair0, 장부$0.012404다. 후보는 앞서16강에 기록한16종이며 API15경기와Champion 저장을 확인했다. 별도 정확 preset `취미 추천해줘`는0.767초/AI0이었다. 이는 CloudFront/API polling 포함 단일 관측이며 브라우저 최초paint나p95측정은 아니다.

임시 공유 점검기의 빈 본문 오류400 이후 AI를 다시 호출하지 않고 DBpreset 후속 경로와 실제브라우저로 공유를 확인했다. 운영총액$0.697184/$5, 실험총액은$5.4206158/$6 그대로다. 속도 개선을 확인했지만 이 한 사례로32강 의미유사성이나 일반입력 정확도가 해결됐다고 판단하지 않는다. 최종배포/게임/공유 범위는 [검증 기록](VERIFICATION.md)을 따른다.
